// ═══════════════════════════════════════════════════════════════
// Pattern Matching in Obfusku
// Demonstrates: Literal patterns, boolean patterns, and wildcards
// ═══════════════════════════════════════════════════════════════

// Integer pattern matching with wildcards
λdescribeNumber(n: ⟁): ⌘ →
    ⟡ n {
        0 → "zero"
        ⟢ 1 → "one"
        ⟢ 2 → "two"
        ⟢ _ → "many"
    }

// Boolean pattern matching
λbooleanToString(b: ○): ⌘ →
    ⟡ b {
        ◉ → "true"
        ⟢ ◎ → "false"
    }

// Nested pattern matching
λclassify(n: ⟁): ⌘ →
    ⟡ (n > 0) {
        ◉ → "positive"
        ⟢ ◎ → ⟡ (n < 0) {
            ◉ → "negative"
            ⟢ ◎ → "zero"
        }
    }

zeroStr ≔ describeNumber(0)
manyStr ≔ describeNumber(42)
boolStr ≔ booleanToString(◉)
classStr ≔ classify(0 ☠︎ 5)

result ≔ classStr
❧
