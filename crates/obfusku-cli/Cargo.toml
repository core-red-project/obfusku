[package]
name = "obfusku-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Obfusku CLI — orchestration and presentation only, no language logic of its own."
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "../../README.md"
keywords.workspace = true
categories.workspace = true

[[bin]]
name = "obfusku"
path = "src/main.rs"

[dependencies]
obfusku-diagnostics = { version = "1.0.0", path = "../obfusku-diagnostics" }
obfusku-core = { version = "1.0.0", path = "../obfusku-core" }
obfusku-typecheck = { version = "1.0.0", path = "../obfusku-typecheck" }
obfusku-syntax = { version = "1.0.0", path = "../obfusku-syntax" }
obfusku-runtime = { version = "1.0.0", path = "../obfusku-runtime" }
obfusku-fmt = { version = "1.0.0", path = "../obfusku-fmt" }
toml = { version = "0.8", default-features = false, features = ["parse"] }

[lints]
workspace = true
